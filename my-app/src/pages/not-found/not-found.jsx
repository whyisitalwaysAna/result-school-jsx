import { EmptyFieldIcon } from '../../components';
import { GoBack } from '../../components';

export const NotFound = () => {
	return (
		<EmptyFieldIcon>
			404 Page not found
			<GoBack />
		</EmptyFieldIcon>
	);
};
